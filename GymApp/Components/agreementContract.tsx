import { Modal, Text, View, Pressable } from "react-native";

const AgreementContract = (props: {isTermsOfService: boolean}) => {
	return (
		<View>

		</View>
	);
}


export default AgreementContract;