import {CameraType, useCameraPermissions} from "expo-camera";

// TODO: Remove Expo Go condition after app is finalized, after we will use react native permissions for both expo camera and webrtc
export default function Permissions() {

}